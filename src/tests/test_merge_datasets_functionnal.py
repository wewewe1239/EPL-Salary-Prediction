import unittest
from merge_datasets import merge_stats_and_salaries


# class TestFunctionnal(unittest.TestCase):
#     def test_merge_stats_and_salaries_should_return_datasets_with_no_NaNs(self):
#         df = merge_stats_and_salaries()
#         assert(len(df) > 400 and ~(df.isnull().values.any()))

